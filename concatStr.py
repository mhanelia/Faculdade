list = [] #Cria uma lista vazia
for i in range(5): #for passando pelo range definido em 5x
    name= input("Nome: ") # Recebe uma entrada de dados e salva na variavel name
    list.append(name) #Pega o que foi inserido na variavel name e adiciona a lista como um novo indice
print(list) #Exibe a lista
